# PR12 Skill Inventory

Baseline: `main@8e0a37e` (PR #11), 19 `skills/*/SKILL.md` entries.

## Reading The Inventory

- Facts are taken from the current `SKILL.md`, bundled files, scripts, prompts, and tests.
- `Public?` is an audit inference, not yet a catalog decision.
- Disposition is preliminary and must be tested against the Phase 1 routing baseline.
- Scope uses `xLLM` for the proven primary path. Other frameworks remain adapter- or artifact-level only.

## Inventory

| Directory / declared name | Trigger and probable category | Inputs | Outputs / artifacts | Bundled files | Dependencies / callers | Framework / backend scope | Public? and negative intents | Overlap | Preliminary disposition and evidence |
|---|---|---|---|---|---|---|---|---|---|
| `ssh-remote-exec` / same | SSH, ProxyJump, container and background execution; support | SSH config, host, command, container | stdout/logs, transferred files, process status | no scripts/references | Used by batch, eval, server | Generic remote host; examples are Windows/Ascend oriented | No normal primary route. Not for experiment, benchmark or diagnosis goals | Duplicated remote snippets in callers | Internalize support; `skills/ssh-remote-exec/SKILL.md`, references from batch/eval/server |
| `xllm-npu-accuracy-debug` / same | Diagnose wrong output or score regression; analyzer/support | prompt, expected/predicted output, logs, commits, A/B artifacts | reproducer, bisect result, root-cause report, validation | 2 references | Called by SOTA, benchmark, eval, accuracy runner | xLLM Ascend; comparisons may use GPU evidence | Yes for diagnosis. Not for simply running CEval | Incident triage includes an accuracy branch; accuracy runner executes measurements | Keep public specialist; `SKILL.md:8-30,52-238` |
| `xllm-npu-accuracy-runner` / same | Execute one EvalScope accuracy workload; runner | API URL, model, mode, run root, work dir | scores, predictions, failed cases, raw EvalScope data | 1 script | Uses server manager; called by eval/SOTA | xLLM OpenAI-compatible service on NPU | Yes only for explicit accuracy execution; not for diagnosis or mixed evaluation | Eval runner includes it; accuracy-debug consumes failures | Keep distinct runner; investigate visibility; `SKILL.md:10-74` |
| `xllm-npu-batch-perf` / same | Repeat service/perf over a model/config matrix; orchestrator | batch config, models, TP, devices, infra | per-model runs, batch config, summary | 1 script, 2 refs | Uses server, perf, report, SSH | xLLM NPU batch execution | Yes for explicit matrix/campaign. Not for one normal perf run or fair comparison | Eval/perf can express each cell; prompt also uses it for repeats | Keep implementation; investigate campaign canonical name/compatibility; `SKILL.md:8-9,227-349` |
| `xllm-npu-benchmark` / same | Fair comparison, SLA search and publishable conclusion; analyzer/orchestrator | completed candidate runs, workload, SLA, fairness policy | normalized results, fairness verdict, comparison report | 4 scripts, 10 refs | Uses eval, profiler, accuracy-debug, report; fairness script called by xllm-flow | xLLM-first; vLLM-Ascend comparison; SGLang declared but not full E2E; Ascend/NVIDIA snapshots | Yes for comparison/review. Not for raw EvalScope-only execution | Competes with eval when request says run-and-compare | Keep public specialist, narrow execution ownership; `SKILL.md:10-23,58-236`, `scripts/xllm_flow.py:1131-1139` |
| `xllm-npu-build-gate` / same | Build plan, execution and binary provenance verdict; gate | repo, framework, commands, build tree, binary, patch | environment, plan, log, fingerprint, provenance, verdict | 2 scripts, 2 refs | Called by eval/SOTA; failure delegates incident | Full xLLM gate; experimental vLLM-Ascend/SGLang build adapters | Direct maintenance/build use is legitimate. Not primary for optimization/eval/incident goals | Incident owns root-cause diagnosis; xllm-flow aggregates verdict | Keep gate with explicit direct route and delegated normal route; `SKILL.md:11-149` |
| `xllm-npu-capacity-planner` / same | HBM/KV/cache/concurrency planning; planner/analyzer | model config, launch args, hardware, logs | capacity JSON, budget, report | 1 reference | Called by SOTA | xLLM/vLLM-Ascend/SGLang analytical scope on NPU | Yes for capacity questions. Not for observed crash diagnosis without budgeting intent | Incident covers OOM; profiler has memory table | Keep public specialist; `SKILL.md:10-55` |
| `xllm-npu-code-review` / same | NPU-specialized patch review; support/analyzer | diff, repository rules, validation evidence | severity-ranked findings and assessment | 2 references | Called by SOTA | xLLM Ascend code | Yes for explicit review. Not for implementation or generic Git review | SOTA review phase | Keep public specialist; `SKILL.md:8-20,135-184` |
| `xllm-npu-compute-simulation` / same | FLOPs/MFU/lower-bound and what-if modeling; planner/analyzer | model config, workload shape, topology, optional profile | estimate JSON, MFU table, what-if | 1 reference | Called by SOTA | Analytical xLLM/vLLM-Ascend/SGLang on Ascend | Yes for modeling. Not for measured benchmark conclusions | Profiler explains measured time; capacity models memory | Keep public specialist; `SKILL.md:10-46` |
| `xllm-npu-eval-runner` / same | Service plus mixed performance/accuracy EvalScope orchestration; orchestrator | model/binary/API/TP/devices/workload/run root | manifest, env, service, perf, accuracy, report, evidence verdict | 1 reference, no own script | Uses build, server, perf, accuracy, report; referenced by benchmark/SOTA | Primarily xLLM; can coordinate separate OpenAI-compatible containers | Yes for explicit mixed/end-to-end evaluation. Not for service-only, comparison, profiling or diagnosis | Competes with perf/accuracy/server and benchmark | Keep orchestrator, narrow primary intents; `SKILL.md:8-28,28-48,84-281` |
| `xllm-npu-incident-triage` / same | Runtime/build incident diagnosis; analyzer/support | logs, system/NPU state, build/service evidence | reproducer, root cause, fix validation, incident report | 4 references | Uses profiler/build evidence; called by build/eval/SOTA | xLLM Ascend | Yes for crash/hang/HCCL/OOM/graph/build failure. Not for dataset score or normal performance analysis | Accuracy and profiler branches overlap specialists | Keep public with negative intents and escalation; `SKILL.md:8-31,67-231` |
| `xllm-npu-perf-runner` / same | Execute one EvalScope performance workload; runner | API/model/tokenizer/concurrency/tokens/warmup | raw EvalScope data and metrics | 1 script | Uses server; called by eval/batch/SOTA | xLLM OpenAI-compatible service on NPU | Yes only for explicit perf-only execution. Not for optimization, fairness or profiling | Eval/batch orchestrate it; benchmark consumes output | Keep distinct runner; investigate visibility; `SKILL.md:10-83` |
| `xllm-npu-pipeline-analysis` / same | Stage/layer/rank/decode-bubble explanation; analyzer | profiling artifact, workload, commit, source map | stage/bubble/rank tables and analysis JSON | 1 script, 1 ref | Consumes profiler artifacts; called by profiler/SOTA | Artifact analysis for xLLM/vLLM-Ascend/SGLang; xLLM boundaries most concrete | Yes for existing-trace pipeline questions. Not generic profile capture | Profiler performs generic five-table analysis | Keep separate public analyzer; `SKILL.md:8-93` |
| `xllm-npu-profiler` / same | Profile collection plus generic five-table analysis; runner/analyzer | service PID or trace, workload, framework | PROF trace, five tables, bottleneck summary, evidence | 7 scripts, 4 refs | Escalates pipeline claims; used by SOTA/benchmark/incident | xLLM and vLLM-Ascend Ascend traces; no verified SGLang workflow | Yes for capture/general profile. Not fair benchmark or detailed pipeline-only question | Collection and analysis combined; pipeline is deeper analyzer | Keep; no split without routing evidence; `SKILL.md:8-65,65-315` |
| `xllm-npu-report-writer` / same | Render caller-owned report template; support | run root, template path, report type | report or batch summary | no scripts/references | Called by eval/batch/benchmark | Artifact-format generic, examples xLLM NPU | Narrow direct intent exists for report from completed artifacts. Not primary during broader experiment | Embedded report steps in orchestrators | Keep support; investigate public vs delegated visibility; `SKILL.md:10-38,87-132,270-279` |
| `xllm-npu-server-manager` / same | Launch/ready/smoke/stop/cleanup lifecycle; runner/gate | binary, model, devices, ports, run root | immutable service attempt and lifecycle JSON | 4 scripts | Uses SSH; called by eval/perf/accuracy/batch | xLLM launcher; OpenAI-compatible checks; Ascend cleanup | Direct launch/stop/health maintenance is legitimate. Not primary for full eval/optimization | Eval smoke prompt incorrectly selects eval runner | Keep direct maintenance route plus delegated normal route; `SKILL.md:10-15,15-158` |
| `xllm-npu-sota-loop` / same | Open-ended performance optimization evidence loop; orchestrator/planner | goal, target, experiment spec, baseline, source | budget, ranked candidates, patches, benchmark/profiling evidence, ledgers | 6 refs | Calls most analysis, execution, review and development skills; directly invokes xllm-flow | xLLM NPU optimization | Yes for measurable multi-round optimization. Not fixed analysis, one benchmark or eval-only request | Competes broadly because it also owns lifecycle text | Keep public; investigate clearer canonical performance-loop name and lifecycle facade; `SKILL.md:8-19,19-272` |
| `xllm-npu-triton-migration` / same | Migrate Triton-Ascend operator to AOT form; support/development | source kernel, target repo, shapes/dtypes, interface | kernel, npubin, wrappers, tests, report | 2 refs | May hand off xllm_ops integration; used by SOTA | Triton-Ascend to torch_npu_ops/xLLM | Yes for explicit migration. Not runtime wiring of an existing op | Adjacent to xllm_ops integration, boundary is explicit | Keep public specialist; `SKILL.md:13-40,40-146` |
| `xllm-npu-xllm-ops-integration` / same | Wire existing xllm_ops into runtime; support/development | existing op, wrapper, callsite, xLLM root | runtime wrapper/callsite/CMake/tests/report | 1 script, 1 ref | Follows migration; used by SOTA | xLLM runtime only | Yes for explicit integration. Not source-kernel migration | Adjacent migration skill, no merge evidence | Keep public specialist; `SKILL.md:8-35,35-114` |

## Control-Plane Implementations That Are Not Skills

| Implementation | Role | Evidence | Disposition |
|---|---|---|---|
| `scripts/xllm_flow.py` | task/run lifecycle, identity projection, checkpoint, gate aggregation, finalize/archive | `scripts/README.md:22-81`; direct calls at `xllm_flow.py:1090-1139` | Keep deterministic implementation; add a public skill facade only if routing baseline supports it |
| `scripts/validate_run_evidence.py` | evidence schema and artifact validation | `scripts/README.md:14,94-101`; tests in `test_validate_run_evidence.py` | Internal script with direct debug CLI, not a public skill |
| `skills/xllm-npu-benchmark/scripts/benchmark_fairness_gate.py` | candidate fairness verdict | called by `xllm_flow.py:1131-1139`; dedicated tests | Internal gate implementation under benchmark ownership |
| `capture_fairness_snapshot.py` | normalized host/device snapshots | benchmark references and tests | Internal snapshot helper, not a skill |
| checksum, fingerprint, projection functions in `xllm_flow.py` | lifecycle integrity | `scripts/xllm_flow.py`, `tests/test_xllm_flow.py` | Internal functions; do not wrap as skills |

## Skill-Like Entry Outside `skills/`

`reference/pr_history/SKILL.md` is validated by `tests/test_repository_hygiene.py` and presents
itself as a skill, but `scripts/init_xllm_workspace.py` installs only direct children of
`skills/`. Its documented packaged query path also differs from the actual top-level
`scripts/query.py`. It is therefore outside the 19-skill inventory required by the Issue but is
install-unreachable and must be covered by catalog/install decisions before Definition of Done.

## Phase 0 Answers

1. No public skill corresponds exactly to the mandatory `xllm-flow` lifecycle.
2. SOTA is an open-ended performance-optimization orchestrator and planner, but currently repeats lifecycle ownership.
3. Eval orchestrates mixed execution; perf and accuracy execute one workload type; batch repeats perf over a campaign; benchmark owns fairness and conclusions.
4. Profiler collection/general analysis and pipeline-specific analysis are separate enough to keep.
5. Incident triage has a direct operational intent. Report writer has a narrow direct completed-artifact intent but is usually delegated.
6. Build and service have legitimate direct maintenance routes. Fairness/evidence gates are implementation CLIs, not normal primary skills.
7. Parser/snapshot/checksum/projection remain ordinary deterministic scripts/functions.
8. Do not generalize names from declared scope: xLLM is primary, vLLM-Ascend/SGLang support remains adapter/artifact-specific.
9. Main competing descriptions are SOTA versus benchmark/profiler/pipeline; eval versus perf/accuracy/server; batch versus repeated perf; benchmark versus run-and-compare; incident versus accuracy/profiling.
10. No `skills/*` entry is completely unreachable. `ssh-remote-exec` is support-only; report writer is mostly delegated. The skill-like `reference/pr_history/SKILL.md` is install-unreachable.
